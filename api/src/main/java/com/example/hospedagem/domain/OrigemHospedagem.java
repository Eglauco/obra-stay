package com.example.hospedagem.domain;

/**
 * Como a hospedagem (entrada) foi registrada: pela administração no sistema,
 * ou pelo próprio colaborador no autoatendimento (QR Code).
 */
public enum OrigemHospedagem {
    ADMINISTRACAO,
    AUTOATENDIMENTO
}
